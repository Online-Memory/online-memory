#set( $userId = $ctx.source )
#set( $dataType = $util.dynamodb.toDynamoDBJson("User") )

{
    "version": "2017-02-28",
    "operation": "GetItem",
    "key": {
        "id": $util.dynamodb.toDynamoDBJson($userId),
        "__typename": $dataType
    }
}
