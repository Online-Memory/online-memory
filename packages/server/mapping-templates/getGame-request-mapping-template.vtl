#set( $dataType = $util.dynamodb.toDynamoDBJson("Game") )

{
    "version": "2017-02-28",
    "operation": "GetItem",
    "key": {
        "id": $util.dynamodb.toDynamoDBJson($ctx.args.gameId),
        "__typename": $dataType
    }
}
